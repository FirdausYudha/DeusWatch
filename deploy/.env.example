# Salin ke deploy/.env lalu sesuaikan. File .env (tanpa .example) di-gitignore.
#
#   cp deploy/.env.example deploy/.env

# Password PostgreSQL. WAJIB diganti untuk apa pun selain dev lokal.
POSTGRES_PASSWORD=deuswatch_dev
